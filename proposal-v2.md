# BoatMeter v2 Proposal

## 1. 現在の構成

### 1.1 ルートファイル
- `index.html` - メイン UI の HTML
- `app.js` - メイン画面のセンサ取得・角度表示・GPS 更新ロジック
- `style.css` - メイン UI のスタイル
- `viewer.html` - CSV データを読み込み、地図とグラフを表示する解析画面
- `viewer.js` - `viewer.html` の CSV パース、地図描画、チャート描画、再生制御
- `manifest.json` - PWA マニフェスト
- 画像：`ponam-rear.png`, `ponam-side.png`

### 1.2 機能概要
- デバイスの `deviceorientation` を使ったロール・ピッチの検出
- GPS 位置取得と速度表示
- 記録モード切り替え UI（ただし実装が存在しない部分あり）
- 解析画面で CSV を読み込み、Leaflet 地図と Chart.js グラフを表示

## 2. 問題点

### 2.1 アーキテクチャ・保守性
- すべてがグローバル変数と DOM API に依存し、疎結合ではない
- 単一ファイルに UI 制御・センサ取得・状態管理・画面遷移が混在している
- `viewer.js` も CSV 読み込み、描画、再生処理が一枚岩で分割されていない
- 関数命名・コード整形が不揃いで、可読性が低い
- `toggleRecording()` が参照されているが `app.js` 内に定義がない

### 2.2 データ保存・解析機能
- データ保存機能がほぼ未実装
  - CSV エクスポートや IndexedDB 保存、ローカルストレージ保存がない
- 解析画面は CSV を手動インポートする形で、アプリ内で記録したデータを直接扱えない
- 現行コードはデータスキーマやメタ情報が明示されていない

### 2.3 UX / UI / 機能面
- レイアウトやスタイルは固定的で、将来の画面拡張に弱い
- ビューが `index.html` / `viewer.html` に分かれているだけで、共通コンポーネントの再利用がない
- モード切り替え・状態遷移が DOM とタイマーに直接依存し、テストしにくい
- 画面ローテーションや縦横判定ロジックが散在している

### 2.4 プロジェクト構成・開発環境
- パッケージ管理やビルドシステムが存在しない
- モジュール化されていないため、将来的にライブラリ追加・改修時の影響範囲が大きい
- コードは ES5/ES6 混在で、今後 TypeScript や ESM 化を導入しにくい

## 3. 改善案

### 3.1 モジュール化と責務分離
- `src/` 配下に機能別モジュールを分割
  - `src/app` - センサ／GPS／録音機能
  - `src/viewer` - 解析画面、チャート、地図、再生制御
  - `src/ui` - 共通 UI コンポーネント、レイアウト、ボタン
  - `src/data` - データスキーマ、保存・ロード、エクスポート
  - `src/utils` - 共通ユーティリティ、数値変換、角度正規化
- ビルド済み成果物を `dist/` に出力する構成を採用

### 3.2 データ保存設計
- セッションデータを構造化して保存する層を設計
  - `Session` / `Record` / `TrackPoint` の明確な型定義
- 保存先として最低限以下を検討
  - IndexedDB / Local Storage（アプリ内保存用）
  - CSV エクスポート / インポート（互換性・共有用）
  - PWA 対応時のオフラインキャッシュと永続保存
- 解析画面側から直接保存データにアクセスできる共通ストレージ API

### 3.3 解析機能設計
- 解析機能を「データ読み込み」「集計」「表示」の3層に分離
  - 読み込み：CSV / ローカル保存 / 外部インポート
  - 集計：速度・傾斜・回転レート・走行距離・疲労指標などの算出
  - 表示：地図、グラフ、統計パネル、再生コントロール
- UI 面で「セッション一覧」「フィルター」「比較」「自動レポート」などを将来追加できる余地を確保

### 3.4 デザイン刷新と UI 拡張性
- 共通デザインシステムを定義
  - カラーパレット、タイポグラフィ、ボタン/カード/モーダルなど
- メイン画面と解析画面を共通レイアウトに収斂できるようにする
- 画面遷移を単一ページアプリケーション（SPA）形式にするか、少なくとも共通コンポーネントを再利用
- モバイル／タブレット対応をより明確にし、縦横検出・回転通知を整理

### 3.5 将来の機能追加に向けた拡張性
- 新しいセンサ、外部 BLE / USB デバイス、IMU 連携を追加しやすい構造
- 記録中のメタデータ（コース名・コメント・天候・状態）を保存できるように設計
- 分析アルゴリズムをプラグイン化し、後から新しい指標を追加しやすくする
- 多言語対応、PWA インストール、ネイティブラップ化（Capacitor / TWA）を見据えた構成

## 4. 推奨フォルダ構成

```
BoatMeter/
├── public/
│   ├── index.html
│   ├── viewer.html
│   ├── manifest.json
│   ├── icons/
│   │   ├── icon-192.png
│   │   └── icon-512.png
│   ├── images/
│   │   ├── ponam-rear.png
│   │   └── ponam-side.png
│   └── assets/
│       └── fonts/   # 追加する場合
├── src/
│   ├── app/
│   │   ├── index.js
│   │   ├── sensor.js
│   │   ├── gps.js
│   │   ├── recorder.js
│   │   ├── session.js
│   │   └── ui-controller.js
│   ├── viewer/
│   │   ├── index.js
│   │   ├── csv-loader.js
│   │   ├── map-controller.js
│   │   ├── chart-controller.js
│   │   └── playback.js
│   ├── data/
│   │   ├── storage.js
│   │   ├── export.js
│   │   ├── schema.js
│   │   └── analytics.js
│   ├── ui/
│   │   ├── layout.js
│   │   └── components.js
│   ├── styles/
│   │   ├── globals.css
│   │   └── components.css
│   └── utils/
│       ├── angle.js
│       ├── time.js
│       └── dom.js
├── dist/
├── tests/
│   ├── app.test.js
│   ├── data.test.js
│   └── viewer.test.js
├── package.json
├── package-lock.json
├── README.md
└── proposal-v2.md
```

## 5. 具体的な移行優先度

1. `src/` ベースのモジュール分割とビルド設定を作成
2. データモデルと保存 API を定義し、セッション記録を永続化
3. 解析画面を共通データレイヤーに接続して再生・集計を実現
4. UI/UX を整理し、共通コンポーネントとレスポンシブデザインを確立
5. 追加機能（メタデータ、レポート、比較、外部デバイス対応）を段階的に実装

## 6. まとめ

現行は小規模で動作単位はあるものの、技術的負債と機能拡張の障壁が高い状態です。BoatMeter v2 では以下を目標に進めると良いでしょう。

- 明確な責務分離とモジュール化
- データ保存と解析基盤の整備
- UI の共通化とデザイン刷新
- 将来の機能追加を支える拡張性の高い構成

この提案に沿って、まずは構成とデータモデルを固める段階から進めるのが最適です。
